<Window x:Class="CTTManagement.Views.Dialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CTTManagement.Views"
        mc:Ignorable="d"
        Title="Dialog" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="TableSettingsDialogFormV" Grid.Column="0"  Margin="10">
           
        </ListBox>
        <ListBox x:Name="TableSettingsDialogTestCases" Grid.Column="1"  Margin="10">
            
        </ListBox>
        <Button Grid.Row="1" Grid.ColumnSpan="2" Content="Show" ></Button>
    </Grid>
</Window>
